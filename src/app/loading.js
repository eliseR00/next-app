export default function Loading() {
	return (
		<main style={{ padding: "3rem 1.5rem", textAlign: "center" }}>
			<h2 style={{ marginBottom: "0.5rem" }}>Loading profiles...</h2>
			<p>Please wait while data is being fetched.</p>
		</main>
	);
}